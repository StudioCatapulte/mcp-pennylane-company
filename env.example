# Configuration MCP Pennylane
# Copier ce fichier vers .env et remplir avec vos valeurs

# === AUTHENTIFICATION ===

# Clé API Pennylane (obligatoire pour les clients)
# Obtenir depuis : Pennylane > Paramètres > API
PENNYLANE_API_KEY=

# OAuth 2.0 (pour les partenaires uniquement)
# PENNYLANE_OAUTH_CLIENT_ID=
# PENNYLANE_OAUTH_CLIENT_SECRET=

# === CONFIGURATION API ===

# URL de base de l'API (optionnel, par défaut : production)
# PENNYLANE_BASE_URL=https://app.pennylane.com/api/external/v2

# Timeout des requêtes API en secondes (optionnel, défaut : 30)
# PENNYLANE_API_TIMEOUT=30

# Nombre maximum de tentatives en cas d'erreur (optionnel, défaut : 3)
# PENNYLANE_API_MAX_RETRIES=3

# === CONFIGURATION SERVEUR MCP ===

# Nom du serveur MCP (optionnel)
# PENNYLANE_MCP_SERVER_NAME=Pennylane MCP

# Version du serveur (optionnel)
# PENNYLANE_MCP_SERVER_VERSION=0.1.0

# Niveau de log : DEBUG, INFO, WARNING, ERROR (optionnel, défaut : INFO)
# PENNYLANE_LOG_LEVEL=INFO 